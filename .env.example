# Database (public defaults — ok to commit)
DB_NAME=cinebh
DB_USERNAME=cinebh_app
DB_PASSWORD=cinebh123

# MinIO / S3 storage (public defaults — ok to commit)
STORAGE_ACCESS_KEY=minioadmin
STORAGE_SECRET_KEY=minioadmin

# --- SECRETS (set by Jenkins, do NOT put values here) ---
# KEYSTORE_PASSWORD=
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# JWT_SECRET=
# SMTP2GO_PASSWORD=
